apiVersion: v1
kind: ConfigMap
metadata:
  name: worker-server-config
  namespace: rapidconnect
data:
  NODE_ENV: "production"

  # Redis
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"
  REDIS_CHANNEL: "channel1"

  # MySQL
  MYSQL_HOST: "db"
  MYSQL_DATABASE: "mydb"
  MYSQL_TABLE: "mytable"
